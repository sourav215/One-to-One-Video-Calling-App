import { Box, Button } from "@chakra-ui/react";

function Logout() {
  return (
    <Box p={"30"}>
      <Button colorScheme="teal" variant="solid" >
        Logout
      </Button>
      </Box>
  );
}
export default Logout;
